---
run_id: 2026-08-31T132322Z-implement-28
stage: implement
started_at: 2026-08-31T13:23:22Z
finished_at: 2026-08-31T14:41:00Z
status: succeeded
issue: 28
pull_request: 37
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# FQ-28 — slice 3b, digest delivery

## What was claimed

Issue #28, tests 16 and 17 of the FQ-3 integration suite. Won the deterministic remote
branch `claude/fq-28` (no prior ref existed), then replaced `factory:ready-to-implement`
with `factory:in-progress` and re-read the issue to confirm the write.

The live handoff was the 2026-08-31T08:54Z `factory-handoff:v1` comment, the fourth on the
issue. Earlier handoffs on #28 carried an open question — whether to spawn the entrypoint
(option A) or construct `PurviewService`/`SlackBridge` inline in a test body (option B) —
and that comment records a human closing it in favour of A. Had the run read the
2026-08-31T06:06Z comment as live instead, it would have stopped on an open human question
that was already answered. The latest comment is the handoff; the earlier ones are history.

Back-pressure was checked before claiming: zero issues at `factory:awaiting-review` and
zero open pull requests, against the charter's limit of 3.

## What changed

One new file, `tests/integration/digest-delivery.integration.test.ts`. Nothing else. No
file under `src/` and no pre-existing test file was modified, so Charter §3 is satisfied by
addition rather than by approval.

252 changed code lines against the 400 ceiling, measured rather than estimated:

```
git diff --numstat origin/main...HEAD -- . ':!docs/**' ':!*.md' | awk '{ a += $1; r += $2 } END { print a + r }'
```

Both tests drive the spawned entrypoint from `harness/server-proc.ts` with
`DIGEST_INTERVAL_MS` set, because `PurviewService.flushDigest` has no wire trigger: no MCP
tool and no HTTP route reaches it, and `harness/purview.ts` does not expose the service it
builds. The child process therefore owns the digest cadence on a real timer.

## What was verified

Gates at the required level:

```
FACTORY_GATES: level=full status=GREEN passed=4 failed=0 failing=none skipped=none misconfigured=none
```

`prove-test.sh` returns `PROOF: status=MISCONFIGURED reason=no-non-test-change` here, which
is the correct answer for a test-only diff and is not a pass. The bite was established
instead by mutating `src/` three ways and watching the tests fail, restoring the tree and
confirming `git status --porcelain` clean after each:

| Mutation | Expected failure | Result |
|---|---|---|
| `escalate`'s `band !== 'digest'` guard always posts | test 16, individual-card assertion | caught |
| `flushDigest` sees an empty pending list | tests 16 and 17, digest waits | caught |
| `digestBlocks` re-attaches buttons to resolved entries | test 17, buttons assertion | caught, and test 16 correctly still passes |

The third mutation is the PR #1 verifier defect reconstructed. Test 17 exists to re-pin it
at the altitude where it originally hid.

`factory-verifier` reached `accepted` in a fresh context, having re-run gates and repeated
all three mutations independently rather than taking this account of them. It additionally
confirmed the `isDigest` discriminator is a total partition of webhook deliveries (so the
absence assertion cannot pass vacuously and fails closed on a post with no blocks), that
the severity arithmetic in the test's comments is correct against `src/domain/severity.ts`,
and that five consecutive integration runs passed with test 16 at 544ms against a 5s budget
and test 17 at 1545ms against 10s.

Gate 3's standing stop condition was not tripped: no sleep or retry was added beyond
`server-proc.ts`'s single `EADDRINUSE` retry, so no `src/` config-extraction issue is owed.

## Noted, not acted on

Two untracked files, `.tmp-probe.mts` and `.tmp-probe2.mts`, appeared in the working
directory at 14:27Z during this run and disappeared before the commit. They are probe
scripts for `workitem://.../tree` resources, which is slice 4 (#9) territory, not this
slice's. They were left alone and are not in the commit.

They imply a second session was operating in this same checkout, on this branch. The
deterministic remote-branch claim in the contract prevents two runs claiming the same
*issue*; it does nothing about two runs sharing one working tree, where a branch switch or
a mutation-and-restore cycle by either one can corrupt the other's state silently. Worth a
human deciding whether concurrent local runs need a worktree per run. Not filed as an issue
from inside the run that observed it.
