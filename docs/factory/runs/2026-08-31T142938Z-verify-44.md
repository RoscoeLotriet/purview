---
run_id: 2026-08-31T142938Z-verify-44
stage: verify
started_at: 2026-08-31T14:26:00Z
finished_at: 2026-08-31T14:29:38Z
status: succeeded
issue: 39
pull_request: 44
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# Verify run — PR #44, FQ-39 slice 4b (concurrent agents)

## Gate level

Diff touches `tests/integration/concurrent-agents.integration.test.ts` and
`docs/factory/runs/2026-08-31T141716Z-implement-39.md`. Neither matches a `LOAD_BEARING`
glob, so per charter §6 this is a `default` change, required level `full`.

Checked out `origin/claude/fq-39` (`ccd7607`), ran `pnpm install`, then
`./.claude/scripts/gates.sh full`:

```
FACTORY_GATES: level=full status=GREEN passed=4 failed=0 failing=none skipped=none misconfigured=none
```

Byte-identical to the PR body's line. No mismatch.

## Checks the gates can't make

**Test proves the fix.** Test-only diff, no non-test hunk in the committed change, so
literal `prove-test.sh` has nothing to revert (this is the same gap noted in the #37 verify
record: the script's non-test-file classifier would instead try to revert the accompanying
`docs/factory/runs/**` commit, which has no runtime effect, and misreport). Verified the
underlying claim by hand instead: reproduced all four of the PR's claimed `src/` mutations
independently against the checked-out branch, one at a time, restoring with `git checkout --`
and confirming `git status --porcelain` clean after each.

| Test | Break | Expected failure | Result |
|---|---|---|---|
| 21 | `canTransition` guard removed from `claim` (`src/service/purview.ts:276-278`) | `won` array length 2 instead of 1 | confirmed — `expected … to have a length of 1 but got 2` |
| 22 | batch pre-check (`assertBudgetFits(parent, batchBudget)`) removed from `fanOut`, per-child check in `createWork` left intact | partial-children state, `children` length 3 instead of 2 | confirmed — `expected … to have a length of 2 but got 3`; `committed` assertion (900 ≤ 1000) stayed green, matching the PR's note that only the length assertion is load-bearing against this defect |
| 23 | first `x-purview-principal` header memoized module-wide in `src/http/app.ts` | bravo's owner id equals alpha's | confirmed — `expected 'pr_…' not to be 'pr_…'` |
| 24 | `settle()` changed to wake every parked waiter instead of the one keyed by escalation id | `stillPending` on the untapped agent settles early | confirmed — `expected the call to still be blocked after 250ms, but it settled` |

Each break was applied on its own; the other three tests were not re-run per break (the PR's
own record already established that independently, and reproducing the 21/22/23/24 mutations
was the part worth re-deriving rather than trusting). All four failures match the PR's table
exactly, including the sharpened test-22 result.

**Pre-existing test files.** Untouched. `git diff --name-status origin/main...HEAD` shows
both changed files as `A` (added); nothing under `src/` or `tests/**` modified.

One inaccuracy in the PR body's own prose, noted but not load-bearing: it says this diff "is
a single A" — by the time the PR body was written (after the run-record commit), the diff is
two `A` entries (the test file and the run record), not one. `done_when` only requires no
file under `src/` be modified, which holds regardless, and the second file is the run record
the contract itself requires every run to write. Not scope creep, just an imprecise sentence.

**Scope.** Exactly the declared `files_expected`
(`tests/integration/concurrent-agents.integration.test.ts`) plus the mandatory
`docs/factory/runs/` record. `git diff --numstat origin/main...HEAD -- . ':!docs/**' ':!*.md'`
gives 280 code lines, matching the PR's claim, well under the charter's 400-line ceiling.

**done_when.** Checked against the live handoff (issue #39, 2026-08-31T13:45:06Z comment):

- Tests 21–24 pass — confirmed in the gate run (112/112 total, this file's 4 included) and by
  the four independent breaks above, each producing exactly the assertion failure the file's
  own test expects.
- The file carries the gate-2 scope note (Node is single-threaded; await-interleaving safety
  only, not thread safety, not multi-process safety) — present verbatim at the top of the
  file, lines 1–13, before any import.
- No file under `src/` is modified — confirmed via `git diff --name-status`.
- Gates report `FACTORY_GATES status=GREEN` at `full` — confirmed, byte-identical to the PR's
  quoted line.

Literally true.

No `.only`/`.skip` in the added file. No new dependency, no config or lockfile change. Real
timers only (`stillPending` in test 24 uses a real `setTimeout`-backed sleep, not fake
timers).

## What was not done

No `factory-critic` pass: no `LOAD_BEARING` glob touched, and the diff is not suspiciously
clean — the PR's own mutation table was independently reproduced rather than accepted on its
word, which is the substance a critic pass would otherwise supply.
