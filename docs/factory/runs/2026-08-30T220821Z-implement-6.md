---
run_id: 2026-08-30T220821Z-implement-6
stage: implement
started_at: 2026-08-30T22:08:21Z
finished_at: 2026-08-30T22:18:07Z
status: succeeded
issue: 6
pull_request: pending
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: true
---

# FQ-6 · slice 1 — round-trip edge cases

Interactive run. The human selected issue #6 explicitly.

## Queue state before the run

#6 was `factory:wait-to-implement`, blocked on slice 0b (#18). Nothing was
`factory:ready-to-implement` and nothing was `factory:awaiting-review`.

**The blocker had cleared and the label had not caught up.** #18 merged to `main` as
`0e43740`, landing `harness/{sign,slack-fake,tap}.ts` and the round-trip test this slice
pushes on. With the review queue empty, Charter §7's back-pressure limit did not apply
either.

So the run re-triaged before claiming: the existing `factory-handoff:v1` comment on #6 was
**edited in place** to `disposition: ready-to-implement`, naming the merge commit that
released it, and the label moved `wait-to-implement` → `ready-to-implement`. Editing in
place rather than appending a second handoff is what the contract requires, and it is the
thing the #18 run recorded as outstanding against itself — see PR #24's "human read
required", point 1.

**This is the part a human should look at.** Promoting an item is a triage decision, and
this run made it inside an implement run because the human named the item. The blocker
being genuinely merged is checkable; whether an implement run should promote its own item
is a policy question this run did not have standing to settle.

## Claim

Deterministic branch `claude/fq-6` from `origin/main`. Claim commit `641fec8` carrying this
run id pushed without force and created the ref, so the claim was won. Label then moved to
`factory:in-progress` and re-read to confirm the write.

## Load-bearing check, done independently of triage

`tests/integration/escalation-edge-cases.integration.test.ts` matches no Charter §2
`LOAD_BEARING` glob. It matches §3 `TESTS_PROTECTED` (`tests/**`), but it is a **new** file,
and §3 covers modification, not addition. No existing file of any kind was touched.

## What changed

One added file, four tests, 247 code lines against the 400 ceiling:

- **2** — the resolved card replaces the original at `response_url` with
  `replace_original: true` and the actions block withdrawn.
- **3** — an unsigned tap does not release the agent, asserted with `stillPending`.
- **4** — a correctly signed tap naming an option that was never offered leaves the
  escalation open.
- **5** — with no tap, the agent's call returns `resolution: 'timed_out'`, and `abort` is
  applied rather than merely recorded: the item ends `failed`.

Tests 3 and 4 each follow the negative assertion with a *successful* tap, so "still blocked"
is distinguished from "quietly closed" and from "that payload was malformed for some
unrelated reason". Without that, both non-release proofs would be satisfiable by a system
that had broken taps entirely.

## Gates

```
FACTORY_GATES: level=full status=GREEN passed=4 failed=0 failing=none skipped=none misconfigured=none
```

Green on the first run. Skipped gates: none. Full suite: 15 files, 92 tests.

## Verification

`factory-verifier`, fresh context, given the base SHA and the queue item rather than an
account of the work. Verdict **accepted**.

> All four tests are present in the one expected new file, each is killed by targeted `src/`
> mutations and by nothing else, and the diff adds a single 247-line test file with no
> `src/`, dependency, or protected-file changes.

`prove-test.sh` is **vacuous on a test-only slice** — with no non-test hunks to revert it
proves nothing — so teeth were established by mutating `src/` instead and restoring the tree
after each. The implementer ran four mutations; the verifier independently ran ten, each
killing exactly one test and no other. Its mutation of `src/http/app.ts` to answer 401 while
still processing the tap is the sharpest of them: it is precisely the failure a status-code
assertion would miss, and it is why `done_when` names `stillPending`.

The verifier ran the integration project three consecutive times without flake.

## Estimate variance, for the spec's running tally

`04-slices.md` estimated this slice at ~180 code lines. Actual: **247, +37%**. That is the
second measured estimate in FQ-3 and the second one high (slice 0 was +56%). Under the
ceiling with room, so no split was needed — but the spec's "estimates are not budget"
section now has two data points pointing the same way, and the remaining slice 2 estimate of
~280 should be read accordingly.

## Reservations carried into review

From the verifier, none of them code defects:

1. Test 4's "open, not merely unreleased" half fails, under a silently-closing mutation, by
   exhausting the 20s test timeout. Correct, but a reviewer who breaks it sees a bare
   timeout rather than a named assertion.
2. Both non-release proofs use a 500ms window. A release slower than that would pass. The
   mitigation is the follow-up signed tap in each test, not the window.

## Not done

Nothing in this slice's scope. Slices 2–5 (#7, #8, #9, #10) remain
`factory:wait-to-implement`; #9 (slice 4) is the one that could be promoted next, since
tests 18–23 need only slice 0a.
