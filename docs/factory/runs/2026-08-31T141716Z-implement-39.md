---
run_id: 2026-08-31T141716Z-implement-39
stage: implement
started_at: 2026-08-31T14:17:16Z
finished_at: 2026-08-31T14:26:00Z
status: succeeded
issue: 39
pull_request: pending
gate_level: full
gate_status: GREEN
verifier: accepted
human_required: false
---

# FQ-39 — slice 4b, concurrent agents over one shared service

Tests 21–24 of the FQ-3 integration suite, in one new file. This is the concurrency half of
the slice FQ-9 wrote whole and then parked at 488 lines against Charter §7's 400-line
ceiling; the split into 4a and 4b was taken at gate 4 revision 3, and with 4a landed in #43
this closes FQ-3's slice 4.

`src/http/app.ts:29-35` builds a new protocol server per POST against one shared
`PurviewService`. That is the normal deployment condition and nothing tested it.

## What changed

One added file. Nothing modified, nothing under `src/`, no dependency or config change.

| File | Lines | Tests |
|---|---|---|
| `tests/integration/concurrent-agents.integration.test.ts` | 280 | 21, 22, 23, 24 |

Measured, not estimated, with the charter's own command:

```bash
git diff --numstat origin/main...HEAD -- . ':!docs/**' ':!*.md' \
  | awk '{ a += $1; r += $2 } END { print a + r }'
# 280
```

The file is a clean cherry-pick of the concurrency half of `claude/fq-9` at `547a4ac`. The
issue allowed re-deriving it and called that wasteful. The verifier independently confirmed
the file is byte-identical to the parked blob, which is the evidence for the spec's claim
that the split ran along the file boundary and moved or trimmed no test. The parked branch
stays parked and must not become a PR.

## Gates

```
FACTORY_GATES: level=full status=GREEN passed=4 failed=0 failing=none skipped=none misconfigured=none
```

Skipped gates: none. 112 tests across 22 files. Gates went red zero times.

## done_when, clause by clause

| Clause | Status |
|---|---|
| tests 21–24 pass | met — 4/4 |
| the file carries the gate-2 scope note | met — lines 1–12, before any import |
| the note says await-interleaving only, not thread safety, not multi-process | met, in those words |
| no file under `src/` is modified | met — `git diff --name-status` is a single `A` |
| gates GREEN at `full` | met |
| diff within the charter ceiling | met — 280 / 400 |

## The negative proof

A test-only slice has no non-test hunk, so `./.factory/scripts/prove-test.sh` has nothing to
revert. The substitute is the one set at slice 4a (`2026-08-31T135945Z-implement-38.md`):
break `src/` on a clean committed branch, confirm the test catches it, restore with
`git checkout --`, confirm the tree is clean. Each of the four tests was broken on its own,
and in every case the other three stayed green — so each test has teeth independently rather
than the file passing or failing as a block.

| Test | Break | Failure |
|---|---|---|
| 21 | `canTransition` guard removed from `claim` (`src/service/purview.ts:276-278`) | `expected [ … ] to have a length of 1 but got 2` |
| 22 | `fitsWithin` oversubscription guard removed | `expected [] to have a length of 1` |
| 23 | first `x-purview-principal` header memoized module-wide in `src/http/app.ts` | `expected 'pr_b0bec646' not to be 'pr_b0bec646'` |
| 24 | any tap wakes every parked waiter instead of the keyed one | `expected the call to still be blocked after 250ms, but it settled` |

The verifier devised and ran its own four breaks in a fresh context rather than repeating
these, and reached the same conclusion. Two of its results sharpen the record:

- On test 22 it removed the **batch** pre-check in `fanOut` and left the per-child check in
  `createWork`. The second batch then commits one child and throws — exactly the partial-
  children state the issue called worse than a refusal. The test fails on
  `expect(children).toHaveLength(2)`, which is the assertion the issue demanded.
- Under that same break `expect(committed).toBeLessThanOrEqual(1000)` did **not** fire —
  900 ≤ 1000 — so the length assertion is what carries test 22. Both properties the issue
  asked for are asserted; only one of them is load-bearing against this defect.

## Verifier

`accepted`, `must_fix: none`. It re-ran full gates itself and reproduced the quoted
`FACTORY_GATES` line, re-measured the diff against the ceiling, and confirmed scope, the
single `A` in `--name-status`, no new dependencies, no fake timers, and no `.only`/`.skip`.

Its notes for the human reader, none of which changed the verdict:

- Test 24 has a second failure mode worth knowing about. Waking the *wrong* waiter, rather
  than every waiter, leaves the other `callTool` parked, so the test fails as a 20-second
  vitest timeout instead of an assertion. A genuine failure, but slow and much less legible
  than the `stillPending` message. Anyone debugging a red test 24 should suspect the waiter
  map before suspecting a flaky timeout.
- Test bodies drive the system only through the MCP client and `/slack/interactions`. No
  direct service calls.

## The scope note is load-bearing prose

Node is single-threaded. These four tests prove safety across `await` interleaving and
nothing more. They are not evidence of thread safety and not evidence of multi-process
safety, and cannot be while v0 has no shared store to contend over. The note sits at the top
of the file because that is where it will be read. Deleting it would take the file out of
`done_when` even with all four tests green.

## Not done

Nothing in scope was left out. With #38 merged and this open, FQ-3 has no slices remaining
that were unblocked at the start of this run.
